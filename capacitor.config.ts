import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sanzgiri.threeclues',
  appName: 'Three Clues',
  webDir: 'dist/public'
};

export default config;
